// Arquivo: js/config.js

// No seu computador local é assim. 
// Quando fizermos o Deploy na nuvem, mudaremos apenas esta linha!
const API_URL = "http://localhost:8081";

console.log('Configurações carregadas. API apontando para:', API_URL);